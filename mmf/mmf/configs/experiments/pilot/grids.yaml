includes:
- ./defaults.yaml

env:
  save_dir: ./save/models/pilot_grids

# only use the images
dataset_config:
  okvqa:
    depth_first: false
    fast_read: true
    use_images: true
    use_features: false
    zoo_requirements:
    - okvqa.defaults
    images:
      train:
      - okvqa/defaults/images/
      val:
      - okvqa/defaults/images/
      test:
      - okvqa/defaults/images/

model_config:
  pilot:
    # models from grid-feats-vqa < https://github.com/facebookresearch/grid-feats-vqa >
    image_encoder:
      type: grid_feats_vqa
      params:
        name: grid_feats_vqa
        # specify backbone and type of features
        # choices: [R-50-(grid or updn), X-101-grid, X-152-(grid or challenge(MoVie+GridFeat))]
        model: R-50-grid.yaml
        output_dir: ${env.save_dir}
        pretrained: true
        zero_init_residual: false
        num_output_features: 1 # per grid?
